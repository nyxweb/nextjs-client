import { Container } from './styles';

import ServerInfo from './children/ServerInfo';

const RightSide = () => {
  return (
    <Container>
      <ServerInfo />
    </Container>
  );
};

export default RightSide;
